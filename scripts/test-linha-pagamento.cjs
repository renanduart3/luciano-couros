const assert = require('node:assert/strict');
const { buildSync } = require('esbuild');
const Module = require('node:module');
const fs = require('node:fs');
const React = require('react');
const {renderToStaticMarkup} = require('react-dom/server');
const result = buildSync({stdin:{contents:'export { LinhaPagamento } from "./src/components/LinhaPagamento"; export { TitulosPagamentoEditor } from "./src/components/TitulosPagamentoEditor"; export { ResumoFinanceiroFixo } from "./src/components/ResumoFinanceiroFixo"; export * from "./src/lib/financeiro";',resolveDir:process.cwd(),loader:'ts'},bundle:true,platform:'node',format:'cjs',packages:'external',write:false});
const mod = new Module(__filename);mod.filename=__filename;mod.paths=module.paths;mod._compile(result.outputFiles[0].text,__filename);
const {recebidoDaLinha,financeiroOrdem,calcularFinanceiroVale,LinhaPagamento,TitulosPagamentoEditor,ResumoFinanceiroFixo}=mod.exports;
const p={id:'p',status:'ativo',statusPagamento:'aguardando',data:'2026-09-22',formaPagamento:'cheque_emitente',valorRecebido:900,valorAplicado:100,valorAplicadoOrdem:100,bonusUtilizado:0,bonusGerado:800,
 alocacoes:[{vendaId:'a',valor:25},{vendaId:'b',valor:75}],titulos:[{valor:300,status:'compensado'},{valor:600,status:'aguardando'}]};
assert.equal(recebidoDaLinha(p,'a'),75);assert.equal(recebidoDaLinha(p,'b'),225);
assert.equal(recebidoDaLinha(p,undefined,'o'),300);
assert.equal(recebidoDaLinha(p,'a'),calcularFinanceiroVale({id:'a',totalLiquido:25,valorPago:25,recebimentos:[p]}).recebido);
assert.equal(recebidoDaLinha(p,undefined,'o'),financeiroOrdem({id:'o',totalOriginal:100,pagamentos:[p]}).recebido);
assert.equal(recebidoDaLinha({...p,status:'recusado'},'a'),0);
assert.equal(recebidoDaLinha({...p,titulos:[],statusPagamento:'compensado',valorRecebido:0,bonusUtilizado:100},'a'),25);
const html=renderToStaticMarkup(React.createElement('table',null,React.createElement('tbody',null,React.createElement(LinhaPagamento,{pagamento:p,vendaIdContexto:'a',clienteId:'c',clienteNome:'Teste',saldo:0,alocar:()=>[],referencia:'vale',onSaved:async()=>{},onDetalhes:()=>{},onComprovante:()=>{}}))));
assert.equal((html.match(/<td /g)||[]).length,6);
assert.ok(html.includes('75,00'));assert.ok(!html.includes('Abatido'));
assert.ok(html.includes('aria-label="Excluir"'));assert.ok(html.includes('data-label="Recebido"'));assert.ok(html.includes('payment-actions'));
assert.ok(html.includes('aria-label="Comprovante"'));assert.ok(html.includes('data-recebimento-id="p"'));
assert.ok(!html.includes('>Comprovante</button>'));
const titulo={nomeTitular:'Fulano',documentoTitular:'12345678909',valor:100,vencimento:'2026-10-10',numeroDocumento:'ABC',observacao:'Teste',status:'aguardando'};
const renderEditor=(formaPagamento)=>renderToStaticMarkup(React.createElement(TitulosPagamentoEditor,{formaPagamento,clienteId:'c',clienteNome:'Cliente',clienteDocumento:'12345678909',valorPagamento:100,titulos:[titulo],onChange:()=>{}}));
const chequeHtml=renderEditor('cheque_emitente');
const boletoHtml=renderEditor('duplicata_emitente');
const ordemEsperada=['>Nome<','>CPF/CNPJ<','>Valor (R$)<','>Vencimento<','>Nº cheque<','>Obs.<'];
for(let i=1;i<ordemEsperada.length;i++) assert.ok(chequeHtml.indexOf(ordemEsperada[i-1])<chequeHtml.indexOf(ordemEsperada[i]));
assert.ok(chequeHtml.includes('>Nº cheque<'));assert.ok(!chequeHtml.includes('>Nº boleto<'));
assert.ok(boletoHtml.includes('>Nº boleto<'));assert.ok(!boletoHtml.includes('>Nº cheque<'));
assert.ok(chequeHtml.includes('aria-label="Observação, linha 1"'));
const resumoHtml=renderToStaticMarkup(React.createElement(ResumoFinanceiroFixo,{negociado:1000,rotuloTotal:'Devedor',financeiro:{negociado:1000,recebido:200,aguardando:400,presumido:600,restante:800,restantePresumido:400,bonus:0,excedentePresumido:0,creditoUtilizado:0}}));
assert.ok(resumoHtml.includes('>Devedor<'));assert.ok(resumoHtml.includes('>Pago<'));assert.ok(resumoHtml.includes('>Restante<'));
assert.ok(!resumoHtml.includes('>Entrou<'));assert.ok(!resumoHtml.includes('Pago presumido'));
assert.ok(resumoHtml.includes('R$\u00a0600,00'));assert.ok(resumoHtml.includes('R$\u00a0400,00'));
const valeFonte=fs.readFileSync('src/components/ValeDetalhesModal.tsx','utf8');
assert.ok(!valeFonte.includes('O excedente foi registrado como crédito na carteira do cliente'));
const ordemFonte=fs.readFileSync('src/components/OrdensCobrancaView.tsx','utf8');
assert.ok(!ordemFonte.includes('Valores dos recebimentos vinculados'));
assert.ok(ordemFonte.includes('Total pago'));
assert.ok(!ordemFonte.includes('Restante: {formatCurrency(financeiroOrdem(ordem).restante)}'));
console.log('OK: coluna recebida por vale/ordem, parcial, rateio, recusa, credito e acoes acessiveis por icones.');
